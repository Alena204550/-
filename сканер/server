мпортный  сокет

sock  =  сокет . сокет ()
носок . привязать (( '' , 9090 ))
носок . слушать ( 0 )
conn , addr  =  sock . принять ()
печать ( адрес )

msg  =  ''

в то время как  True :
	данные  =  соед . recv ( 1024 )
	если  не  данные :
		ломать
	msg  + =  данные . декодировать ()
	соед . отправить ( данные )

печать ( сообщение )

соед . закрыть ()
